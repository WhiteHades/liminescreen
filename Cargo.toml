[workspace]
members = ["firmware", "manager"]
default-members = ["manager"]
resolver = "3"

[workspace.package]
# Keep 0.1.0 until the owner explicitly requests a version change.
version = "0.1.0"

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
